1. npm init -y    // create the package json file
2. npm i express
3."type":"module"  // in package.json file we need to use this for using import method instead of require method. "commonjs" is the defualt method
4."main": "index.mjs" // has to be mjs cus of the module type
5."start:dev": "nodemon ./src/index.mjs", // in package.json -  under scripts, has to mention these
    "start": "node ./src/index.mjs"
6.npm i express-validator